{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m api.rapidapi --serve",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}